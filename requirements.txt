# --- Núcleo (requerido para correr el backend y los tests) ---
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
pydantic-settings>=2.2
sqlalchemy>=2.0
cryptography>=41.0
httpx>=0.27
python-multipart>=0.0.9

# --- Base de datos ---
psycopg2-binary>=2.9          # driver PostgreSQL

# --- Integraciones externas (necesarias para las llamadas de voz reales) ---
twilio>=9.0                   # WhatsApp Business / Programmable Voice / Media Streams
openai>=1.30                  # Realtime API (audio-to-audio)
websockets>=12.0             # WebSocket cliente hacia la Realtime API

# --- Orquestación de agentes (opcional; hay fallback secuencial sin ella) ---
# langgraph>=0.1

# --- Desarrollo / testing ---
pytest>=8.0
